server:
  port: 8001
spring:
  profiles:
    active: native,default
  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/idea-workspace/SpringCloudConfigrepository/website
  application:
    name: WebSite-root