spring:
  application:
    name: website
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: website-config
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8002/eureka/